# Task 3: Update reviewer.md Agent

## Context

The reviewer agent has references to "Logger (no console.*)" and "*.spec.ts" tests from the serverless repository.

## Goal

Update `.claude/agents/reviewer.md` to be fully Astro-specific.

## Instructions

1. **Read** the current reviewer.md file
2. **Remove** Logger references:
   - In Astro/static sites, `console.*` is acceptable for debugging
   - No custom Logger class in this repo
3. **Update** test references:
   - Testing not yet configured
   - Future: `*.test.ts` or `*.spec.ts` with Vitest
4. **Update** review checklist for:
   - Astro vs Svelte component usage
   - Tailwind dark mode (`dark:` variants)
   - Content Collections frontmatter
   - Proper hydration directives (`client:load`, `client:visible`)
5. **Update** validation commands:
   - `npm run biome:check`
   - `npm run astro:check`

## Key Changes

Replace:
```
Check: no console.* (use Logger), explicit types (no any), *.spec.ts naming, import order.
```

With:
```
Check: explicit types (no any), proper Astro/Svelte patterns, dark mode support, import order per AGENTS.md.
```

## Acceptance Criteria

- [ ] No Logger references
- [ ] Astro/Svelte review checklist
- [ ] Dark mode checking included
- [ ] Hydration directives mentioned

## Completion Log

**Status:** ⏳ Pending
