# Task 8: Update type-fix Skill

## Context

The type-fix skill needs Astro-specific type patterns.

## Goal

Update `.claude/skills/type-fix/SKILL.md` with Astro type patterns.

## Instructions

1. **Read** the current skill file
2. **Add** Astro-specific types:
   - `CollectionEntry<'blog'>` from `astro:content`
   - `APIRoute` from `astro`
   - Component Props interfaces
   - Frontmatter types
3. **Update** validation:
   - `npm run astro:check` (primary for types)
   - `npm run biome:check`
4. **Update** examples:
   - Fixing Astro component props
   - Typing Content Collection queries
   - API route typing

## Key Types

Common Astro types:
```typescript
import type { CollectionEntry } from 'astro:content';
import type { APIRoute } from 'astro';

interface Props {
  title: string;
  lang?: string;
}
```

## Acceptance Criteria

- [ ] Astro-specific types documented
- [ ] Content Collection types included
- [ ] `npm run astro:check` is primary validation
- [ ] Examples are relevant

## Completion Log

**Status:** ⏳ Pending
